/*used to replace the statement wherre a operator tawks 1 variavle as one of its argument and assignresult back to the same variable */

//x=x+=1 is same as x+=1

//x+=2 means x value is incremented by 2
#include<stdio.h>
void main()
{
    int i;
    for (i=0;i<=10;i+=2)
    {
        if(i==0)
        {
            printf("even numbers ");
            
        }


    }
}